# Render.com deployment configuration
services:
  - type: web
    name: vega-stremio-addon
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
